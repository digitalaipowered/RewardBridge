# CPX App 34813 — production contract

## Application

- App ID: `34813`
- App name: `RewardBridge Network`
- CPX dashboard status: `Active`
- Integration: API Mobile + Web
- Active site: `https://6028ef2dad7c557eb4.v2.appdeploy.ai/`
- Managed partner traffic remains disabled inside RewardBridge until the platform-level approval gate is deliberately enabled.

## Exact main postback URL

Paste this single line into CPX **Main Postback URL**:

```text
https://fmywfaffczulebozlpsx.supabase.co/functions/v1/cpx-postback?status={status}&trans_id={trans_id}&user_id={user_id}&sub_id={subid}&sub_id_2={subid_2}&amount_local={amount_local}&amount_usd={amount_usd}&offer_id={offer_ID}&type={type}&hash={secure_hash}&ip_click={ip_click}
```

`type={type}` is intentionally included even though CPX's short example omits it. CPX states that the type identifies `out`, `complete`, or `bonus`; RewardBridge uses it to prevent screen-outs from creating payable transactions.

## Verified CPX rules

- `status=1`: completed
- `status=2`: canceled
- A completion later invalidated for fraud is sent again with the same transaction ID and `status=2`.
- Postback secure hash: `MD5(trans_id + "-" + app_secure_hash)`.
- Main Postback URL is mandatory.
- CPX's dashboard test calls the main URL for a screen-out event.
- CPX-listed postback source IPs:
  - `188.40.3.73`
  - `157.90.97.92`
  - `2a01:4f8:d0a:30ff::2`

## Receiver behavior

The active Supabase `cpx-postback` Edge Function:

1. Accepts GET or POST callbacks.
2. Requires a CPX-listed source IP.
3. Validates `MD5(trans_id-app_secure_hash)` using the private `CPX_SECURE_HASH` Edge Function secret.
4. Redacts the received hash before persisting the raw callback.
5. Treats `out` and other non-payable event types as successful no-op callbacks.
6. Creates pending financial entries only for `status=1` and `type=complete` or `type=bonus`.
7. Passes 100% of CPX bonus revenue to the end user.
8. Treats a later `status=2` as a compensating reversal, not a duplicate.
9. Uses the transaction ID plus status and type for callback idempotency.
10. Verifies `subid_1` and `subid_2` against the mapped RewardBridge project.
11. Cannot release balances while the managed-network switch is disabled.

## Required private secret

In Supabase Dashboard → Edge Functions → Secrets, set:

```text
CPX_SECURE_HASH=<the App Secure Hash shown for CPX App 34813>
```

Do not put this value in GitHub, AppDeploy files, InfinityFree files, frontend JavaScript, screenshots, or support messages.

`CPX_POSTBACK_TOKEN` is not used by the exact CPX receiver. CPX authenticates callbacks with its source-IP list and `{secure_hash}`.

## Exact CPX reward settings

Enter these values in CPX **Reward Settings**:

```text
Currency Name (Singular): Reward Credit
Currency Name (Plural):   Reward Credits
Currency Factor:          0.45
Currency Decimal Places:  2
Currency Factor for Bonus: 1.00
Bonus Decimal Places:      2
```

This means:

```text
$1.00 of CPX publisher payout = 0.45 Reward Credits for a completed survey
$1.00 of CPX bonus payout     = 1.00 Reward Credit for the user
```

One Reward Credit represents $1.00 of RewardBridge reward value after the provider payment is received, validated, and released. Pending credits are not withdrawable cash.

## Survey-list request

RewardBridge calls CPX server-side with:

```text
https://live-api.cpx-research.com/api/get-surveys.php?app_id=34813&ext_user_id={stable_rewardbridge_user_id}&subid_1={project_cpx_subid}&subid_2={project_slug}&output_method=api&ip_user={actual_user_ip}&user_agent={url_encoded_user_agent}&limit=12&secure_hash={md5(ext_user_id-CPX_SECURE_HASH)}
```

The browser must not construct this request. The private hash is generated by the `cpx-surveys` Edge Function. Survey results are refreshed no sooner than every 120 seconds and RewardBridge prefers CPX's `href_new` user-specific link.

## Completion reward economics

The authoritative gross amount is CPX `amount_usd` in the postback and `payout_publisher_usd` in the survey-list response.

For each valid completed-survey gross amount:

- RewardBridge platform fee: `25%`
- Risk reserve: `10%`
- End-user reward: `45%`
- Publisher margin: the remaining `20%`

End-user rewards are floored to whole cents so the displayed reward can never exceed the amount credited.

Examples:

| CPX gross | Platform fee | Reserve | End user | Publisher margin |
|---:|---:|---:|---:|---:|
| $1.00 | $0.25 | $0.10 | $0.45 | $0.20 |
| $2.00 | $0.50 | $0.20 | $0.90 | $0.40 |
| $5.00 | $1.25 | $0.50 | $2.25 | $1.00 |

For a CPX `bonus` event, the platform fee, reserve, and publisher margin are zero and the user receives 100% of the bonus amount.

All provider callbacks first create pending entries. They become available only after the corresponding CPX payment is received and reconciled.

## Payout limits

- Publisher withdrawal minimum: `$25.00`
- Publisher-controlled end-user minimum: `$2.00` or higher

## Safe test sequence

1. Add `CPX_SECURE_HASH` to Supabase Edge Function secrets.
2. Enter and save the exact Reward Settings values above.
3. Paste the exact Main Postback URL.
4. Save the CPX Postback Settings.
5. Click **Click here to test Postback** once.
6. Confirm the test returns success in CPX.
7. Check `provider_postback_tests` and `provider_events` in Supabase.
8. Do not enable the managed-network switch as part of the postback test.
